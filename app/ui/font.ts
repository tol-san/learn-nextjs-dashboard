import { Inter, Lusitana } from 'next/font/google'

export const inter = Inter({ subsets: ['latin'] })

// Lusitana requires explicit weights — available: 400, 700
export const lusitana = Lusitana({ subsets: ['latin'], weight: ['400'] })



